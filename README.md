# Arduino's AVR-GCC15.1 toolchain

This repo contains a copy of Microchip's AVR-GCC 15.1 toolchains (where the docs and GDB has been removed) for different platforms. Unfortunately, ARM64 for Linux is missing, but may be added in the near future.

